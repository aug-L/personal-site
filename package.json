{
  "name": "personal-site",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "w-dev": "wrangler dev",
    "w-deploy": "npx wrangler deploy",
    "w-build-deploy": "npx astro build && npx wrangler deploy",
    "generate-types": "wrangler types",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^13.5.1",
    "@astrojs/sitemap": "^3.7.2",
    "@tailwindcss/vite": "^4.3.0",
    "astro": "^6.3.3",
    "tailwindcss": "^4.3.0"
  },
  "devDependencies": {
    "wrangler": "^4.91.0"
  },
  "overrides": {
    "vite": "^7"
  }
}